node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# 빌드 산출물(.ics + index.html)은 GitHub Actions가 직접 GitHub Pages로 배포하므로 git에 안 넣음
public/

# pnpm 디버그 로그
.pnpm-debug.log*

# IDE
.vscode/
.idea/

# 로컬 캡처용 API fixture (시점 데이터라 stale될 수 있음)
fixtures/

# main.ts가 작성하는 fallback 발동 marker (GitHub Actions가 같은 워크플로 안에서 읽음)
.fetch-source

# Claude Code 세션 캐시·worktree (개발 도구 임시 디렉터리)
.claude/
